# Personal access token used by /api/stats.
# Classic token: no scopes needed for public data; add `repo` only if you want
# private contribution counts included. Fine-grained token: read-only, public
# repositories. The GitHub GraphQL API rejects anonymous requests, so this is
# required for the stats card (the other two cards need nothing).
GITHUB_TOKEN=ghp_replace_me
